/**
 * A second interface, so a class can show it implements MULTIPLE interfaces:
 *     class Circle implements Shape, Drawable, Comparable<Circle>
 * (A class can extend only ONE class but implement MANY interfaces.)
 */
public interface Drawable {
    void draw();
}
